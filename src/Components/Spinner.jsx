import React from 'react';

const Spinner = () => {
    return (
        <div>
                    <span className="loading loading-spinner w-16 h-16 text-green-600"></span>
        </div>
    );
};

export default Spinner;